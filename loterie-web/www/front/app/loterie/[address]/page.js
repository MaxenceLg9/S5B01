'use server'
import {ContractButton} from "@/app/components/loterie/contract";


export default async function Page({ params }) {
    return (
        <div>
            <ContractButton/>
        </div>
    );
}